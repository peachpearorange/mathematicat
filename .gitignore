# Downloaded UKMT PDFs and the pipeline's intermediates. Bulky, and the papers
# are not ours to redistribute.
import/pdf/
import/*.json
import/*.log

__pycache__/
*.pyc

# Holds the folded API key. Never commit this.
key.js
